// import { useRestoMenus } from "./useRestoMenu";

// export const useOrderRestoMenus = (restaurants: { restaurantId: number }[]) => {
//   return restaurants.reduce<Record<number, ReturnType<typeof useRestoMenus>>>((acc, r) => {
//     acc[r.restaurantId] = useRestoMenus(r.restaurantId);
//     return acc;
//   }, {});
// };
